def inc_backup(i_user, i_pwd, i_dstDir):
    print(__name__, i_user, i_pwd, i_dstDir, 'call the internal inc funcion')
    
def full_backup(i_user, i_pwd, i_dstDir):
    print(__name__, i_user, i_pwd, i_dstDir, 'call the internal full funcion')
    
def del_expired_backupset(i_user, i_pwd, i_dstDir):
    print(__name__, i_user, i_pwd, i_dstDir, 'call the internal del funcion')    
        
def up_load_backupset(i_user, i_pwd, i_dstDir):
    print(__name__, i_user, i_pwd, i_dstDir, 'call the internal upload funcion')    
                